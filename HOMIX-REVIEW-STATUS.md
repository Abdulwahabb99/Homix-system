# حالة العمل على ملاحظات مراجعة السيستم

**آخر تحديث:** 2026-08-14

**المستودعات:**
- الباك إند: `/home/garment/homix` — فرع `refactor`
- الفرونت إند: `/home/garment/garment/Homix-system` — فرع `dev`
  (⚠️ `/home/garment/garment/Homix-system-main` نسخة قديمة من 2026-05-02 وليست مستودع git — يُفضَّل حذفها)

**حالة التحقق:** الباك إند 143/143 اختبار ناجح + `tsc` نظيف · الفرونت إند `tsc` نظيف و`build` ناجح.

> **مهم:** السيرفر يعمل من `dist/`، فلن تظهر أي تعديلات باك إند قبل `npm run build` (أو التشغيل بـ `npm run dev`).

---

## ١. ما تم إنجازه

### الطلبات (Orders)

| البند | الحالة | التفاصيل |
|---|---|---|
| ملف تصدير الطلبات | ✅ | أُعيد بناؤه بالأعمدة الـ13 المطلوبة وبنفس الترتيب المحدَّد، مع إضافة مصدر الطلب، التوصيل بواسطة، حالة التأخير، الأولوية، والمبلغ المطلوب تحصيله |
| تعديل تكلفة الشحن والخصم | ✅ | تعديل inline داخل بطاقة «التفاصيل المالية»، والباك إند يعيد حساب `totalPrice` و`toBeCollected` ثم تُعاد قراءة الطلب |
| تحديد الأولوية | ✅ | قائمة اختيار في بطاقة حالة الطلب + في صفحة التعديل |
| تطابق صفحة التعديل مع التفاصيل | ✅ | أُضيفت الحقول الناقصة (حالة التأخير، الأولوية، الشحن، الخصم، جدية الشراء)، وصُحِّح عنوان خاطئ كان يسمّي «حالة التصنيع» بـ«حالة التأخير»، وأصبح «المبلغ المطلوب تحصيله» للقراءة فقط لأن الباك إند يحسبه |
| أخطاء الطلب اليدوي | ✅ | خمسة عيوب: تعطُّل عند منتج بلا مورد، حالة الطلب المختارة كانت تُهمَل تماماً، `shopifyId: "undefined"`، تكرار الأرقام داخل الدفعة الواحدة وإعادة استخدامها بعد الحذف، و`subTotalPrice =` بدل `+=` |
| فاتورة قبل الحفظ | ✅ | زر «فاتورة» في صفحة إنشاء الطلب يفتح نافذة لاختيار الأصناف ثم يُصدِّر PDF بنفس مستند فاتورة الطلب — الشحن وجدية الشراء يُوزَّعان بالنسبة عند اختيار جزء من الأصناف |
| البحث برقم الطلب في الشحنات | ✅ | حقل جديد في شريط فلاتر الشحنات موصول بالـ API |
| التذاكر من داخل الطلب | ✅ | رقم الطلب كان يظهر فارغاً لأن البحث يتم برقم العملية فقط — الآن يُملأ الحقلان من الطلب المُستخرَج ويصبحان للقراءة فقط |

### الشحنات (Shipments)

| البند | الحالة | التفاصيل |
|---|---|---|
| رقم الشحنة | ✅ | `SH-{معرّف داخلي}` ← `SH{رقم الطلب}` (مثال `SH10587`)، مع تفعيل فلتر `shipmentNumber` الذي كان معرَّفاً بلا تنفيذ |
| ملف تصدير الشحنات | ✅ | كان نسخة من تصدير الطلبات — أُعيد بناؤه بالأعمدة الـ16 المطلوبة، وصار **صفاً لكل شحنة** بدل صف لكل بند |
| ملف PDF للشحنة | ✅ | كان `window.print()` للصفحة كاملة — الآن يستخدم مستند فاتورة الطلب نفسه |
| العودة لقائمة الشحنات بعد التأكيد | ✅ | |
| مرتجع المورد | ✅ | نافذة تعديل الحالة والسبب (نفس نافذة مرتجعات العملاء) |

### الفواتير

| البند | الحالة |
|---|---|
| جودة الـ PDF | ✅ PNG بدل JPEG 0.92، ورسم بـ 3x بدل 2x |
| حذف عنوان المعرض | ✅ |
| حذف حالة الفاتورة | ✅ |
| هومكس ← هوميكس | ✅ في كل الفرونت إند |
| كود الطلب = رقم الطلب | ✅ |

### المخزون والحسابات والمرفقات

| البند | الحالة | التفاصيل |
|---|---|---|
| المرفقات | ✅ | كانت تُحفَظ باسم الملف الأصلي، فملفان باسم `invoice.pdf` يطمس أحدهما الآخر وكل السجلات تشير للأخير. الاسم المخزَّن صار فريداً مع بقاء الاسم الأصلي للعرض |
| واجهة المخزون | ✅ | زر «إضافة صنف» + نافذة إضافة/تعديل + حذف. البحث بالكود أو الاسم، والتكلفة واللون والمقاس تأتي من الـ variant المختار ولا تُكتب يدوياً |
| حركة المخزون | ✅ | الطلب الجديد يخصم (FIFO، ويقف عند الصفر)، والمرتجع «فورفيت» يُعيد للمخزون |
| واجهة المصروفات | ✅ | نموذج «إضافة مصروف جديد» + حذف لكل صف |
| حالة المحاسبة للتسليمات | ✅ | كانت تُشتق من حالة الدفع بلا إمكانية تغيير. أُضيفت أعمدة `accountingStatus`/`accountingDate`/`accountingReference` ونقطة `PUT /shipments/accounts/deliveries/:orderId`، والتعديل من داخل **نافذة** لا من الجدول |
| فلاتر التسليمات | ✅ | رقم الطلب، حالة المحاسبة، طريقة الدفع، تاريخ المحاسبة + تطبيق/إعادة ضبط |
| فلاتر المرتجعات | ✅ | كانت مرسومة لكن بعرض 50% فتظهر مزدحمة — صارت بعرض كامل |
| إخفاء التبويبات بلا صلاحية | ✅ | «المخزون» و«الحسابات» تختفيان بدل إظهار خطأ |

### أخطاء اكتُشفت أثناء العمل (لم تكن في الملف الأصلي)

| الخطأ | الحالة |
|---|---|
| تعديل الشحنة يعطي 500 — `shipmentType: ""` يفشل في `isIn` | ✅ كل عمود اختياري يقبل `""` كـ«غير محدَّد» |
| تعديل المرتجع يفشل دائماً | ✅ سببان: القائمة تعرض **معرّف الطلب** حين لا يوجد سجل مرتجع فيذهب الطلب لسجل غير موجود (صار يُنشئ السجل عند أول تعديل)، و`reason: ""` كان يُرفَض رغم أن تفريغه مقصود |
| مفتاح صلاحية غير موجود | ✅ استُخدم `finance_edit` وهو غير معرَّف — صُحِّح إلى `finance_settle` |
| إجراءات لوحة التحكم لا تعمل | ✅ `onClick` كان معطَّلاً بتعليق |
| فشل الميجريشن على `orders_shipping_company_fkey` | ✅ التطبيع كان يتخطى الصفوف المحذوفة منطقياً بينما المفتاح الأجنبي يشمل كل الصفوف |

### الأداء (مقيس، لا تخمين)

| نقطة النهاية | قبل | بعد |
|---|---|---|
| `/orders/summary` | **19,840 ms** | **229 ms** |
| `/shipments/summary` | **17,550 ms** | **369 ms** |

السبب في الحالتين: `findAll` بلا حد على كل الصفوف المطابقة مع شجرة الـ includes كاملة (بنود الطلب ← المنتج ← المورد، العميل، الملاحظات) لحساب ستة أرقام فقط. صار التجميع في SQL عبر `GROUP BY`، مع ضم الجداول فقط حين يشير فلتر إليها.

تحسينات فرونت إند إضافية:
- كل الطفرات الـ18 كانت تُبطِل المفتاح الجذر `["shipments"]` فتُعيد جلب القائمة (1482 صفاً) والملخّص والـ meta والمرتجعات والمخزون والحسابات والتقارير بعد **كل** تعديل — صار الإبطال موجَّهاً لكل قسم
- `refetchOnWindowFocus` كان مفعَّلاً فيُعيد الجلب مع كل عودة لتبويب المتصفح — أُوقِف (`staleTime` 30 ثانية يكفي)
- نافذة الفاتورة في صفحة إنشاء الطلب كانت مركَّبة دائماً وتستقبل كائناً جديداً مع كل ضغطة مفتاح (سبب بطء الكتابة) — صارت مُذكَّرة ولا تُركَّب إلا عند الفتح

### سكربت النشر `db:deploy-refactor`

- كل الميجريشنات والـ backfill في ملف واحد: `src/scripts/run-refactor-deploy-migration.ts`
- أُضيفت الـ26 فهرساً من `migrations/add-indexes.js` (لم تكن موجودة)، وفهارس جديدة: `shippedFromInventory`، المركَّب `(shippedFromInventory, shipmentStatus)`، وأعمدة المحاسبة
- فهارس `pg_trgm` للبحث بـ `LIKE '%...%'` (لا يستفيد من فهارس btree) — تُتخطّى بأمان إذا لم تتوفّر الصلاحية
- **إعادة حساب الغرامات:** كانت `UPDATE` لكل طلب على حدة بانتظار كل رحلة شبكة. صارت مُجمَّعة (500 لكل دفعة)، ومحصورة بآخر 90 يوماً افتراضياً، مع طباعة تقدُّم
  - `--fines-days=N` لتغيير النافذة · `--all-fines` لمسح كامل · `--skip-dashboard-backfill`
- **آمن للإيقاف وإعادة التشغيل:** كل خطوة idempotent ولا توجد معاملة شاملة

---

## ٢. ما يحتاج قراراً منك

### تقارير الأداء — التصميم لا يطابق المطلوب
طلبتَ سابقاً حذف «متوسط التوصيل» و«نسبة النجاح» وقد حُذفا، لكن التصميم الذي أرسلتَه لاحقاً يحتوي عليهما مع عناصر أخرى غير موجودة حالياً:
- بطاقات: متوسط وقت التوصيل · إجمالي GMV · طلبات مسلَّمة هذا الشهر
- تبديل الفترة: يومي / أسبوعي / شهري / حسب التاريخ
- رسم بياني «التسليمات اليومية» بالأعمدة
- جدول «تفاصيل التوصيل حسب شركة الشحن» بأعمدة: عدد الطلبات المسلَّمة · إجمالي GMV · متوسط التوصيل · نسبة النجاح · المرتجعات

**مطلوب تأكيدك:** هل نُعيد بناء الصفحة بالكامل حسب التصميم (بما يشمل إرجاع المقياسين المحذوفين)؟

### «مستعجل جدا» صار يعرض صفراً
بطاقة الطلبات كانت تحسب الطلبات المتأخرة عن موعد التسليم، لأن الكود القديم كان يقصر `attributes` فلا يقرأ `priority` إطلاقاً ويشتقّها من التاريخ. بعد التجميع في SQL صارت تتبع `resolveOrderPriority` فعلياً: الأولوية المخزَّنة لها الأسبقية، وكلها `1` افتراضياً ⇒ النتيجة صفر.

هذا **متسق الآن** مع شارة الأولوية وفلترها في قائمة الطلبات (كانت البطاقة تخالفهما). الخياران:
1. إبقاء الدلالة الحالية (الأولوية اليدوية التي أصبحت متاحة) — الوضع الحالي
2. إرجاع الدلالة القديمة: «متأخر عن موعد التسليم»

---

## ٣. ما لم يُنفَّذ بعد

- **فلاتر المخزون** لا تُرسَل للـ API — تُطبَّق محلياً على الصفحة المعروضة فقط. الباك إند يدعم `productCode`/`vendorName`/`status`
- **`listInventory` و`listReturns` و`listDeliveryAccounts`** ما زالت تجلب كل الصفوف ثم تُقسِّم الصفحات في الذاكرة، و`totalCount` فيها = عدد صفوف الصفحة لا الإجمالي (ترقيم صفحات خاطئ عند تجاوز صفحة واحدة). القياس الحالي مقبول (1–2.5 ث) لأن البيانات قليلة، لكنها ستتدهور مع النمو
- **بطاقات مُجمَّعة محفوظة** (اقتراحك): البنية موجودة للوحة التحكم (`dashboardDailyMetrics` + `DashboardAggregateService`). بعد التجميع في SQL صار الملخّص 229/369 ms، فلم تعد هناك حاجة عاجلة — والجداول المحفوظة تجلب معها خطر انحراف القيم. أقترح تأجيلها ما لم يعُد البطء
- **التحقق الحقيقي من الصلاحيات عبر HTTP**: تتبّعت السلسلة من قاعدة البيانات حتى الـ JSON وكلها ترسل `permissions` (35/35 لـ `admin@homix.com`)، لكنني لم أنفّذ طلب تسجيل دخول فعلي لعدم توفّر كلمة السر. للتأكد بعد تسجيل الدخول:
  ```js
  Object.keys(JSON.parse(localStorage.getItem("user")).permissions || {}).length
  ```
  الناتج `35` يعني أنها تصل سليمة


---

## ٥. الصلاحيات — فجوة مؤكَّدة وأُصلحت

**المشكلة:** داشبورد البائع كانت تعرض «لوحة التحكم» فقط. السبب أن دالة
`getPermissionTemplateForUserType` لم يكن لها قالب للبائع إطلاقاً، بل كائن مكتوب
مباشرةً بثلاثة مفاتيح فقط: `dashboard_view` + `notifications_*`.

**المرجع المستخدَم:** فرع `main` القديم في الفرونت إند (`src/routes.js`)، وهو ما كان
كل دور يراه فعلياً قبل الـ refactor:

| الدور | قوائم main القديمة | القالب قبل الإصلاح | الحالة |
|---|---|---|---|
| admin | الرئيسية · المنتجات · الطلبات · الشحنات · المصانع · تقارير مالية · الموردين · المستخدمين | 35 مفتاحاً | ✅ مطابق |
| **البائع** | الرئيسية · المنتجات · الطلبات · تقارير مالية | **3 مفاتيح فقط** | ❌ ناقص 4 |
| **اللوجستي** | الرئيسية · المنتجات · الطلبات · الشحنات | بلا `products_view` | ❌ ناقص 1 |
| العمليات | الرئيسية · المنتجات · الطلبات · المصانع · الشحنات | 14 مفتاحاً | ✅ مطابق |

كذلك على `develop` لم يكن هناك `isNotVendor` على مسارات المنتجات إطلاقاً، وعلى
الطلبات والشحنات كان على مسارات التعديل فقط — أي أن البائع كان يملك اطّلاعاً واسعاً.

**ما تم:**
1. أُضيف قالب `vendor` حقيقي: `dashboard_view`, `products_view`, `orders_view`,
   `finance_view`, `finance_export`, `tickets_view`, `tickets_reply`, `notifications_*`
2. أُضيف `products_view` لقالب اللوجستي
3. أُضيفت خطوة `repairRolePermissions()` للسكربت — الفحص على قاعدة البيانات أظهر أن
   **الـ82 بائعاً جميعهم** يخزّنون `false` صراحةً لهذه المفاتيح، و`normalizePermissions`
   تُقدّم المخزَّن على القالب، فإصلاح القالب وحده لا يكفي

**نتيجة التشغيل التجريبي (قراءة فقط، بلا كتابة):**

| الدور | المستخدمون | سيُصلَح | مفاتيح ستُضاف |
|---|---|---|---|
| admin | 4 | 0 | 0 |
| **البائع** | 82 | **82** | **492** (6 لكل بائع) |
| العمليات | 5 | 0 | 0 |
| **اللوجستي** | 1 | **1** | **1** |

الخطوة **تمنح فقط ولا تسلب**: أي صلاحية أُوقفت عمداً لمستخدم تبقى موقوفة ما لم يمنحها
قالب دوره، ولا يُمَس أي مفتاح خارج القالب.

> يلزم تشغيل `npm run db:deploy-refactor` حتى تصل الإصلاحات للمستخدمين الحاليين.

### تجمّع الاتصالات (سبب `ConnectionAcquireTimeoutError`)
`Promise.all` غير المحدود على مئات الـ upserts لم يعد موجوداً في المصدر (أُصلح ضمن
إعادة الهيكلة)، فالخطأ جاء من نسخة `dist` أقدم. لكن لم يكن هناك إعداد pool إطلاقاً —
الافتراضي 5 اتصالات ومهلة 60 ثانية. أُضيف إعداد صريح (max 10، acquire 120 ثانية).

### أداء إضافي مقيس
`/shipments/meta` كان **1.3 ثانية في كل نداء** وهو أخفّ نقطة نهاية، ويُطلَب في كل
تبويب. السبب: أربع استعلامات متتابعة (ثلاثة `COUNT` على الطلبات + شركات الشحن) أي
أربع رحلات متتابعة لقاعدة بيانات بعيدة. صارت استعلاماً واحداً مُجمَّعاً `GROUP BY`
مع بقية الاستعلامات بالتوازي، وأُضيفت أعداد حقيقية لتبويبات المخزون والحسابات
والتقارير (كانت تعرض صفراً ثابتاً).

كذلك: استعلاما قائمة الشحنات والملخّص كانا يعملان في **كل** التبويبات (المرتجعات،
المخزون، الحسابات، التقارير) رغم عدم عرضهما — أي جلب 1482 صفاً بلا داعٍ وإعادة جلبها
مع أي إبطال. صارا مقيَّدين بتبويب الشحنات عبر `enabled`.

### الغرامات
النافذة الزمنية (90 يوماً) التي أضفتُها أولاً كانت **خاطئة**: تتخطى بصمت طلبات قديمة
متأخرة تستحق غرامة. استُبدلت بشرط استحقاق فعلي — الطلب يُعاد حسابه إذا كان يحمل غرامة
بالفعل أو كان متأخراً حقاً. النتيجة على البيانات الحقيقية: **817 من 1241** طلباً مفتوحاً،
والاستعلام 458 ms. ويمكن تحديد سقف بـ `--fines-limit=N` (مُرتَّب من الأحدث).

---

## ٦. ملاحظات تشغيلية

1. `npm run build` قبل اختبار أي تعديل باك إند (السيرفر يقرأ من `dist/`)
2. `npm run db:deploy-refactor` لتطبيق الأعمدة والفهارس الجديدة — بدونه لن يعمل حفظ حالة المحاسبة
3. خطأ `[eslint] Plugin "react" was conflicted` في dev server: الإعداد مُصحَّح بالفعل و`build` ينجح — يحتاج إعادة تشغيل السيرفر فقط
4. أربعة ملفات في الفرونت إند (`.eslintrc.json`, `permissionKeys.ts`, `InventoryPanel.tsx`, `ordersMeta.api.ts`) تغيّرت خارج تعديلاتي (تنسيق تلقائي عند الحفظ) وتُركت كما هي
